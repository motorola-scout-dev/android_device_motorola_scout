hal_server_domain(hal_weaver_default, hal_weaver)

init_daemon_domain(hal_weaver_default)

binder_call(hal_weaver_default, secure_element);

allow hal_weaver_default secure_element_service:service_manager find;
